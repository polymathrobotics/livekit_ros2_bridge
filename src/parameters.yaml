livekit_ros2_bridge:
  livekit:
    url:
      type: string
      default_value: ""
      description: "LiveKit server URL."
    token:
      type: string
      default_value: ""
      description: "LiveKit token."
